# 核心 Agent 框架
langgraph>=0.0.10
langchain>=0.1.0
langchain-openai>=0.0.2

# 数据处理与验证
pydantic>=2.0.0

# 环境变量管理 (用于存放未来的 MiMo API Key)
python-dotenv>=1.0.0

# 增强控制台演示效果 (让 main.py 的输出更有高级感)
rich>=13.0.0

# 异步支持
asyncio
